<%- include header %>
<dl class="archive dl-horizontal">
    <p>与您所搜索的关键词<strong><%= keyword %></strong>相关结果:</p>
    <% posts.forEach(function (post, index) { %>
    <dt>
        <time><%= moment(post.meta.updateAt).format('MM/DD/YYYY')%></time>
    </dt>
    <dd>
        <a href="/post/<%= post._id %>"><%= post.title %></a>
    </dd>
    <%})%>
</dl>


<ul class="list-group">

    <li class="list-group-item pull-left">

        与您所搜索的关键词<strong><%= keyword %></strong>相关结果有<span class="badge"><%= posts.length %></span>个:
    </li>
    <% posts.forEach(function (post, index) { %>
    <li class="list-group-item">
        <h4 class="list-group-item-heading"><time><%= moment(post.meta.updateAt).format('MM/DD/YYYY')%></time></h4>
        <p class="list-group-item-text"><a href="/post/<%= post._id %>"><%= post.title %></a></p>
    </li>
    <%})%>
</ul>

<%- include footer %>